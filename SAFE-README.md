# 金庫管理アプリ（日本人管理者用）— Safe Manager

金庫の中身を「おつりボックス」と「売上保管」の2つに分けて管理し、
**いま何セディを何枚足せばいいか**と、**前日の売上をいくら受け取ったか**を把握するアプリ。

## ファイル
| ファイル | 用途 |
|---|---|
| [index.html](index.html) | **アプリ本体。**入口・レジ締め・金庫管理が1ファイルに入っている |
| [gas-backend/Code.gs](gas-backend/Code.gs) | 複数端末で共有するためのバックエンド（Google Apps Script） |

金庫管理（この画面）は **日本人マネージャー**（role: manager）で入口にサインインしないと開けない
（シェフ・ドライバーは入口に戻される）。ドライバー（アブダル・リチャード）は同じデータを見るが、
別の英語・機能限定の画面「🚚 Change & delivery」を使う（下記「配送（ドライバー→オフィス）」参照）。
レジ締め側の説明は [README.md](README.md)。

---

## 全体の流れ

```
【ガーナ・店舗】                          【日本】
スタッフがレジ締め                        金庫管理アプリ
 till-close.html                          safe-manager.html
      │                                        │
      │ 保存すると自動でLark送信                  │
      │ （末尾に同期コードが付く）                 │
      ▼                                        │
  Larkグループ ───── コピペ ──────────────────▶ 反映タブ
                                               │
                                               ▼
                                   おつりボックスの残高が減る（払出）
                                   売上保管の残高が増える（入庫）
                                               │
                                               ▼
                                   「₵1を81枚足す」等が自動で出る
                                   「前日ぶん GH₵ 828 を受け取る」
```

金庫の中身を2つの区画として扱う。

- **🧰 おつりボックス** — 釣り銭用の小額紙幣。スタッフがレジ締め時に持ち出す（減る）／ドライバーが補充する（増える）
- **💰 売上保管** — スタッフが店舗の投入金庫にしまった売上現金。**ドライバーが引き取ると減る**（店舗保管中 → 配送中）
- **🚚 配送中** — ドライバーが店舗から引き取ったが、まだオフィスで受領確認していない現金。**マネージャーが受領確認すると消える**（配送中 → 完了）

つまり売上1件の一生は「店舗保管中 → 配送中 → 受領確認ずみ」の3段階。
ホーム画面の「🗺️ 今どこに・誰がいくら持っているか」で、どの現金が今どの段階にあるかを横断して見られる。

## 配送（ドライバー → オフィス）

1. **ドライバー**が「🚚 Change & delivery」→「Deliver」で、持っていく日にチェックを入れる（何日分かが自動で出る）
2. 実際に数えた紙幣を入力する。**期待額との差はその場でチェック**される（合っていれば ✔、違えば Short/Over と出る）
3. 確定すると、その分は店舗の「売上保管」から消え、「配送中」に変わる。ホーム画面にドライバー名つきで表示される
4. オフィスに着いたら、**日本人マネージャー**が「🚚 配送確認」タブで、実際に受け取った金額を数えて確定
   - ドライバーの引き取り額と食い違えば、差異として記録に残る（履歴・受領ずみ一覧の両方で確認できる）
5. 確定すると「配送中」から消え、完了

ドライバーが複数店舗を回った場合も、店舗ごとに配送を分けて記録すれば、ホーム画面でドライバー1人の合計として横断的に見られる。

### おつりボックスの残高は「実測」が正
毎晩スタッフがレジ締めのSTEP 4でおつり箱を数え、その枚数が同期コードで届く。
アプリはその**実測値を出発点**にして、その後の出入りだけを足し込む。

- 記録もれがあっても、翌日のカウントで自動的に正しい数字に戻る（積み上げ計算だとズレが永久に残る）
- 実測と帳簿が食い違えば、その晩の**差異**として履歴に残り、ホーム画面に警告が出る
- 一番最初のカウントは帳簿がゼロなので「開始残高として登録」と記録され、差異扱いにはならない

---

## セットアップ

### STEP 1 — バックエンドを用意する（複数端末で共有する場合のみ・約10分）

この端末だけで使うなら STEP 1 は飛ばしてよい（アプリはオフラインでも動く）。
複数の日本人が同じ在庫を見るには、以下が必要。

1. [Google スプレッドシート](https://sheets.new)を新規作成し、名前を「KOFIRICE 金庫台帳」などにする
2. メニューの **拡張機能 → Apps Script** を開く
3. 出てきた `コード.gs` の中身を全部消し、[gas-backend/Code.gs](gas-backend/Code.gs) の中身を貼り付ける
4. **1行目の `TOKEN` を推測されにくい文字列に変更する**（例: `kofirice-safe-8f3k2p`）。あとでアプリに入力するのでメモしておく
5. 保存 → 関数のプルダウンから `setup` を選んで **実行**。初回は権限の確認画面が出るので許可する
6. 右上の **デプロイ → 新しいデプロイ**
   - 種類: **ウェブアプリ**
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **全員**
   - → デプロイ
7. 表示される **ウェブアプリURL**（`https://script.google.com/macros/s/.../exec`）をコピー

> **⚠️ 接続キーは店舗端末に入れないこと。**
> 役割による画面の出し分けは端末側のJavaScriptなので、本気で調べれば回避できます。
> 実質的な壁は接続キーです。**キーを持たない店舗端末は、金庫の台帳をそもそも取得できません。**
> 店舗端末はレジ締めだけを行い、結果はLarkの同期コードで管理側に届きます。

> **アクセス「全員」で大丈夫？**
> URLを知らないと開けず、さらに TOKEN が合わないとデータを一切返さない造りになっている。
> URLとTOKENは日本人メンバー以外に共有しないこと。心配なら定期的にTOKENを変えて再デプロイする。

> **Code.gs を直したときは**「デプロイ → デプロイを管理 → 鉛筆アイコン → バージョン：新バージョン → デプロイ」。
> 「新しいデプロイ」を選ぶとURLが変わってしまうので注意。

### STEP 2 — 金庫管理アプリを開く

`safe-manager.html` をブラウザで開く。スマホなら「ホーム画面に追加」でアプリのように使える。

> **注意**: サーバー共有を使う場合、ファイルを直接開く（`file://`）とブラウザの制限で同期できないことがある。
> レジ締めアプリと同じく **GitHub Pages に載せて `https://` で開く**のが確実（下記「公開する」参照）。

1. 右上の ⚙️ → **サーバー接続** に、STEP 1 のウェブアプリURLと接続キー（TOKEN）を入力 → 保存
2. 「✔ 同期ずみ」と出れば成功。他の端末にも同じURL・キーを入れれば同じデータが見える
3. **店舗** で店舗名と**店舗コード**、おつりボックスの**目標在庫**を設定する

### STEP 3 — 開始残高（通常は不要）

**店舗でレジ締めを1回すれば、おつり箱の実測枚数が自動で入る**ので、普通は何もしなくてよい。

レジ締めを待たずに今すぐ残高を知りたい場合だけ、**補充タブ → 「🔍 開始残高を登録する」** から手で数えて登録する。

### STEP 4 — レジ締めアプリ側の店舗コードを合わせる

店舗の端末で `till-close.html` を開き、**Settings → Shop code** に、金庫アプリで設定したのと同じ店舗コード（例 `S2`）を入れて保存。

これで、その店舗のレジ締めが正しい金庫に紐づく。

---

## 毎日の使い方

### ① 反映（レジ締めの記録を取り込む）

1. Larkに届いたレジ締めのメッセージを**まるごとコピー**
2. 金庫アプリの **📥 反映** タブに貼り付け
3. 内容を確認して「✅ 反映する」

メッセージの末尾に `KFR2|...` という1行が入っている。アプリはこれを自動で拾うので、
どこからどこまでコピーするかを気にする必要はない。**複数日ぶんまとめて貼ってもよい**。

同じレジ締めを二度貼っても二重登録されない（「反映ずみ」と表示される）。

### ② ホームで不足を確認

おつりボックスの **不足枚数** が金種ごとに出る。「₵1を81枚」のようにそのまま用意すればよい。

### ③ 補充

**🧰 補充** タブ。不足枚数が最初から入っているので、実際に入れた枚数に直して記録する。

現金の出所を選べる：
- **外部から用意** — 両替・ATMなど、金庫の外から持ってきた場合
- **金庫の売上袋から** — 金庫にある売上現金から小額紙幣を移す場合。売上保管の残高も自動で減る（古い日の入庫から順に引き当てる）

### ④ 売上を受け取る

**💰 受取** タブ。日付ごとに「いくら・何セディが何枚」入っているかが出る。

「受け取る」→ 実際に受け取った枚数を入力 → 確定。
予定と違う枚数を入れると差異が表示される。**少なく入力すれば残りは未受取のまま残る**（分割して受け取れる）。

---

## 画面の見かた

| 表示 | 意味 |
|---|---|
| 🧰 おつりボックス | 釣り銭用の在庫。目標に足りない枚数が赤字で出る |
| 📏 ◯月◯日に実際に数えた枚数がもと | この残高の根拠。カウント後に補充などがあれば件数が付く |
| 🧾 レジに置く枚数 | 店舗のレジ締めアプリに設定されているフロート。店舗側で変更されると通知が出る |
| 💰 売上保管 | まだ受け取っていない売上の合計 |
| ⚠️ 直近のカウントが帳簿と合っていません | **最重要。**おつり箱の実測が計算と違う。金額と担当者が出るので確認する |
| ⚠️ 要確認：差異 | 帳簿と未受取一覧が食い違っている。受取枚数の入力ミスか記録もれ |
| ⚠️ 在庫がマイナス | アプリが知らない持ち出しがあった可能性。棚卸で合わせる |
| 上部のバー | 同期の状態。「未送信 N件」は圏外などでサーバーに送れていない分（オンラインになれば自動送信） |

### 差異が出たときの見かた
1回だけの小さな差異はたいてい数え間違い。**同じ担当者に、同じ向き（いつも不足）の差異が続くとき**が要注意。
履歴タブで「おつり箱カウント（実測）」の行を追うと、いつからズレ始めたかが分かる。

日時はすべて **ガーナ時間（GMT）** で表示している。日本時間だと現地の夜のレジ締めが翌日扱いになり、
「前日ぶんの売上」がどれか分からなくなるため。

---

## 記録の考え方

- 台帳は**追記のみ**。過去の記録は書き換わらないので、あとから監査できる
- 修正したいときは「棚卸」で実数に合わせる。差異が履歴に残る
- 1件の記録は必ず片方の区画だけを増減させる。「売上袋からおつりへ移す」は2件に分けて記録される

## オフライン・複数端末について

- ネットが無くても記録できる。サーバーには未送信として溜まり、オンラインになったら自動で送られる
- 開いている間は25秒ごと、および画面に戻ったタイミングで自動同期する
- 店舗設定を2人が同時に変えた場合は、**先に保存した方が残る**（後から保存した端末には警告が出て、サーバー側の内容に上書きされる）
- 設定 → バックアップ から、全データをJSONで書き出し／読み込みできる

---

## 公開する（GitHub Pages）

レジ締めアプリと同じ `miiiyoshi71one-sudo/kofirice-till` リポジトリに置ける。

```bash
gh repo clone miiiyoshi71one-sudo/kofirice-till "$TEMP/kofirice-till-app"
```

`safe-manager.html` をそのままリポジトリ直下にコピーして push すると
`https://miiiyoshi71one-sudo.github.io/kofirice-till/safe-manager.html` で開ける。

> **公開範囲について**: このHTMLファイル自体には金額も接続情報も入っていない
> （ウェブアプリURL・接続キーは各端末に保存され、ファイルには書き込まれない）。
> ただし公開リポジトリなので、**Code.gs の TOKEN やウェブアプリURLは絶対にコミットしない**こと。

`till-close.html` の更新も同じリポジトリの `index.html` を差し替えて push する。
（同期コード対応のため、今回 `till-close.html` も更新している。反映しないと同期コードが出ない）

---

## 同期コードの仕様（参考）

```
KFR2|S2|1785704400|Evelyn|T1x20|S100x1,50x4|B50x8,20x15,1x39|F50x5,20x5,1x30|C500|M140|P500|Xbb9j
```

| 位置 | 内容 |
|---|---|
| `KFR2` | 形式のバージョン（`KFR1` の古いコードも読める） |
| `S2` | 店舗コード |
| `1785704400` | レジ締め日時（UNIX秒） |
| `Evelyn` | 担当スタッフ |
| `T…` | 金庫から取り出した紙幣（おつり払出） |
| `S…` | 金庫に入れた紙幣（売上入庫） |
| `B…` | **おつり箱の実測枚数** |
| `F…` | **レジに残す枚数（フロート設定）** |
| `C…` / `M…` | 現金売上 / MoMo売上 |
| `P…` | **POSで申告された現金売上** |
| `X…` | 検査値（コピー切れ検出用） |

`T-` のように `-` は「該当なし」。検査値が合わないとアプリが「途中で切れています」と教える。
